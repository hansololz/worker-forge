# Add the worker's runtime dependencies here, one per line, pinned to a
# specific version. Workers are frozen artifacts — a version that worked
# at forge time should work forever.
#
# Common additions:
#   requests==2.32.3
#   pypdf==4.3.1
#   feedparser==6.0.11
#   beautifulsoup4==4.12.3
#   lxml==5.3.0
#
# Only add what this specific worker actually uses. Empty by default —
# the runtime itself uses only the Python standard library.
